{
  "name": "@formation/common",
  "version": "2.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "mocha --require ts-node/register \"**/*.test.ts\"",
    "coverage": "nyc -r html -r text yarn test"
  }
}
